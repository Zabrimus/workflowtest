name: Build Main

on:
  workflow_dispatch:

jobs:
  build-arm:
    uses: ./.github/workflows/build-subworkflow.yml
    with:
      runner: ubuntu-22.04-arm

  build-x86:
    uses: ./.github/workflows/build-subworkflow.yml
    with:
      runner: ubuntu-22.04
